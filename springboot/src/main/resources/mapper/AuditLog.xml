<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="usc.doctor_workstation_system.springboot.mapper.AuditLogMapper">
    <insert id="insert">
        insert into audit_log (record_id, user_id, username, action, detail)
        values (#{recordId}, #{userId}, #{username}, #{action}, #{detail})
    </insert>
    <select id="listByRecordId" resultType="usc.doctor_workstation_system.springboot.entity.AuditLog">
        select * from audit_log where record_id = #{recordId} order by created_at desc
    </select>
</mapper>
